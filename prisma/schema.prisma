generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model channel {
  id           Int     @id @default(autoincrement())
  d_server_id  String? @db.VarChar(32)
  youtube_id   String? @db.VarChar(64)
  d_channel_id String? @db.VarChar(32)
}

model test {
  id          Int     @id @default(autoincrement())
  test        String? @db.VarChar(32)
}